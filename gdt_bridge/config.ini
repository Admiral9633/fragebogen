# GDT Bridge – Konfigurationsdatei

[bridge]

# ── GDT-Ordner (lokal auf diesem Rechner) ──────────────────────────────────
gdt_inbox  = C:\GDT\inbox
gdt_outbox = C:\GDT\outbox

# ── Django Server ───────────────────────────────────────────────────────────
api_url = http://192.168.178.84:8000/api
api_key = ETjqCZtM00I0Xwt-N5wi6i_jmUijkw2dh6g7F5F2mSk

# Fragebogen-Template (leer = neuester aktiver wird automatisch gewählt)
template_slug =

# ── Polling-Intervalle ──────────────────────────────────────────────────────
poll_inbox_seconds  = 5
poll_result_seconds = 30
